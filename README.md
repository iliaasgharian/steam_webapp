# Steam Game Info & Analytics Website

Scaffold structure only — files are empty placeholders, ready for implementation.

## Backend (FastAPI)
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload

## Frontend (React)
cd frontend
npm install
npm run dev
